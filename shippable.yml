language: c

compiler:
  - gcc
  - clang

build:
  pre_ci_boot:
    image_name: drydock/u16cppall
    image_tag: master
    pull: true  
    
  pre_ci:
    - echo 'true'

  ci:
    - gcc --version
    #- ./configure && make && make install
    #- ./hello

  post_ci:
    - echo "true"
